import React from 'react'

const IntroSlidesTitle = ({children}) => {
  return (
    <div className='intro-slides-title'>
      <div className='intro-slides'>
        {children}
      </div>
    </div>
  )
}

export default IntroSlidesTitle
